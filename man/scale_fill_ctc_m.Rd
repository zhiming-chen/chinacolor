% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10.scale_fill_color_ctc_m.R
\name{scale_fill_ctc_m}
\alias{scale_fill_ctc_m}
\title{Custom Fill Color Scale Using China Traditional Colors}
\usage{
scale_fill_ctc_m(palette = NULL, color_pick = NULL, direction = 1, ...)
}
\arguments{
\item{palette}{A vector of hex color codes (ignored if \code{color_pick} is provided).}

\item{color_pick}{A list generated by \code{create_color_pick()}, specifying:
\itemize{
\item \code{color_id}: Directly selected color IDs (optional).
\item \code{group_info}: List with \code{group} (vector of group numbers) and \code{subgroup} (list of subgroups per group) (optional).
\item \code{order}: Sorting rule (1: input order; 0: ascending ID; -1: descending ID).
}}

\item{direction}{1 (default) for original color order, -1 to reverse.}

\item{...}{Additional arguments passed to \code{ggplot2::scale_fill_manual()}.}
}
\value{
A ggplot2 fill scale object.
}
\description{
Creates a ggplot2 fill scale using colors from \code{chinacolor} dataset,
with color selection controlled by a \code{color_pick} list generated by \code{create_color_pick()}.
}
\examples{
\dontrun{
# Step 1: Generate color_pick using create_color_pick()
my_pick <- create_color_pick(
  groups = c(3, 5),          # Select color groups 3 and 5
  subgroups = list(c(1, 3), 2),  # Subgroups 1&3 for group 3; subgroup 2 for group 5
  order_rule = 0             # Sort by color ID ascending
)

# Step 2: Use in ggplot with scale_fill_ctc_m
ggplot(iris, aes(Sepal.Length, Sepal.Width, fill = Species)) +
  geom_point(shape = 21, size = 3) +
  scale_fill_ctc_m(color_pick = my_pick)  # Pass the generated color_pick
}

}
\seealso{
\code{create_color_pick()} for generating \code{color_pick};
\code{ctc_palette()} for how colors are selected from \code{chinacolor}.
}
